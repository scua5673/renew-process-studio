# Static PWA; production publishing waits for the exact commit's browser CI.
[build]
  publish = "dist"
  command = "npm test && npm run test:sql && node scripts/verify-release.mjs && node scripts/package-site.mjs"
[build.environment]
  NODE_VERSION = "22"
